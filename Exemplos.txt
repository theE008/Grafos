/** // Grafo triangular

    Grafo G = novo_Grafo  (true);

    Vertice v = novo_Vertice (G);
    Vertice w = novo_Vertice (G);
    Vertice x = novo_Vertice (G);
    
    Aresta v_w = nova_Aresta (G, v, w);
    Aresta w_x = nova_Aresta (G, w, x);
    Aresta x_v = nova_Aresta (G, x, v);

    imprimir_Grafo (G);

    limpar_Grafo (G);
*/